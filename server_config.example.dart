/// The schema to be used for the calls ('http' or 'https')
const kServerSchema = 'http';

/// API address
///
/// If running on development, this probably is your machine's ip address.
const kServerHost = '';

/// API port
///
/// If running on development, this probably is 4450.
const kServerPort = 4450;
